#include<stdio.h>
#include<string.h>
int main(void)
{
    char a[]="poojan";
    char b[]="sarth";
    // char c[99];
    printf("The strcmp of a and b is %d \n",strcmp(a,b));
    // puts(a);
    // puts(b);
    // puts(strcat(a,b));
    // printf("The length of a is %d\n",strlen(a));
    // printf("The length of b is %d\n",strlen(b));
    //printf("The reverse of a is: ");
    //puts(strrev(a));
    //printf("The reverse of b is: ");
    //puts(strrev(b));
    // strcpy(c,strcmp(a,b));
    // puts(c);       
}